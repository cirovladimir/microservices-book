server.port: 7001
spring.data.mongodb:
  host: localhost
  port: 27017
  database: product-db
logging:
    level:
        root: info
        org.springframework.data.mongodb.core.MongoTemplate: debug
